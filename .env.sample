# Server
PORT=5055
DB_PATH=./data.db
UPLOADS_DIR=./uploads
NODE_ENV=production

# Twilio (optional; leave blank to skip SMS)
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_FROM_NUMBER=

# Client (Vite) — put this in client/.env.local locally or set as a Railway/Render env var
# VITE_GOOGLE_MAPS_API_KEY=your_google_static_maps_key
